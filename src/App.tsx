import { BrowserRouter } from "react-router-dom";
import Layout from "./layouts";

const App = () => {
  return (
    <BrowserRouter>
      <Layout />
    </BrowserRouter>
  );
};

export default App;
